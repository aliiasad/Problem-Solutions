for (int i = 0; i < abs(topLeftY); i++) {
    //     for (int j = abs(topLeftX); j < 10; j++) {  // j = abs(topLeftX)
    //         std::cout << p_count[i][j] << " ";
    //     }
    //     std::cout << std::endl;
    // }

    // for (int i = abs(topLeftY); i < 10; i++)    {   // i = abs(topLeftY)
    //     for (int j = 0; j < abs(topLeftX); j++) { // j < abs(topLeftX)
    //         std::cout << p_count[i][j] << " ";
    //     }
    //     std::cout << std::endl;
    // }

    // for (int i = abs(topLeftY); i < 10; i++)   { // i = abs(topLeftY)
    //     for (int j = abs(topLeftX); j < 10;j++)   { //j = abs(topLeftX)
    //         std::cout << p_count[i][j] << " "; 
    //     }
    //     std::cout << std::endl;
    // }